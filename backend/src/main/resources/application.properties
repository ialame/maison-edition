# Application
spring.application.name=maison-edition
server.port=8080

# MariaDB Configuration
spring.datasource.url=jdbc:mariadb://localhost:3306/maison_edition
spring.datasource.username=ia
spring.datasource.password=foufafou
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
spring.jpa.properties.hibernate.format_sql=true

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=uploads

# JWT Configuration
app.jwt.secret=VotreCleSecreteTresLongueEtSecurisee123456789ABCDEF
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=http://localhost:5173

# OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
